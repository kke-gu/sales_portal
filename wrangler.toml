name = "sales-portal"
main = "src/index.js"
compatibility_date = "2024-01-01"

[env.production]
name = "sales-portal"

[env.staging]
name = "sales-portal-staging"

# 빌드 설정
[build]
command = ""
cwd = "."

# Pages 설정 (정적 파일 서빙용)
pages_build_output_dir = "."

# 환경 변수 (필요시 추가)
# [vars]
# ENVIRONMENT = "production"

# KV 네임스페이스 (필요시 추가)
# [[kv_namespaces]]
# binding = "ESTIMATES"
# id = "your-kv-namespace-id"

# D1 데이터베이스 (필요시 추가)
# [[d1_databases]]
# binding = "DB"
# database_name = "sales-portal-db"
# database_id = "your-database-id"

